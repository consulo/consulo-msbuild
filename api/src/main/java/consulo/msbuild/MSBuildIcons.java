package consulo.msbuild;

import consulo.msbuild.icon.MSBuildIconGroup;
import consulo.ui.image.Image;

@Deprecated
public interface MSBuildIcons
{
	Image Msbuild = MSBuildIconGroup.msbuild();
	Image VisualStudio = MSBuildIconGroup.visualstudio();
}